# Data Cleaning & Analysis Pipeline (README)

Цей README описує мою роботу як аналітика даних над датасетом *UK-500*, що містить контактну інформацію британських компаній та фізичних осіб. Проєкт включає повний цикл роботи з даними: від завантаження та очищення — до створення нових ознак, аналізу, групування та експорту результатів.

---

##  1. Імпорт даних та первинне дослідження

На початковому етапі я:

- імпортувала дані з відкритого джерела (`uk-500.csv`);
- провела первинну діагностику структури:
  - перегляд перших рядків (`head()`),
  - інформація про типи даних (`info()`),
  - базова статистика (`describe()`),
  - пошук пропусків та дублікатів.

💡 *Мета:* оцінити якість даних та зрозуміти, який обсяг очищення потрібен.

---

## 2. Очищення даних

Очищення даних проводилось комплексно:

### 🔸 2.1. Уніфікація текстових колонок
- очищення пробілів (`str.strip()`);
- приведення до нижнього/верхнього регістру (для email, web, імен тощо);
- стандартизація написання через `str.title()` (імена, міста);
- нормалізація адрес:  
  - видалення зайвих пробілів через регулярні вирази,
  - приведення до стандартного формату.

### 🔸 2.2. Очищення email та веб-сайтів
- приведення до нижнього регістру,
- видалення зайвих символів та пробілів.

### 🔸 2.3. Стандартизація телефонів  
Створила функцію `clean_phone()`, яка:
- видаляє всі нецифрові символи,
- зберігає знак “+” (якщо був),
- повертає `NaN`, якщо номер не містить цифр.

Додатково використала регулярні вирази (`\D+`) для очищення `phone1` та `phone2`.

### 🔸 2.4. Приведення всіх текстових колонок до єдиного формату
Створено універсальну функцію `standartize_text()`, яка:
- перетворює будь-який тип у рядок (за потреби),
- видаляє дублікати пробілів,
- коректно обробляє пропуски.

---

## 3. Створення нових ознак (Feature Engineering)

Я додала такі колонki:

### ✔ `full_name`
Комбінація імені та прізвища.

### ✔ `email_domain`
Виділення домена із e-mail.

### ✔ `city_length`
Довжина назви міста (корисно для текстового аналізу).

### ✔ `is_gmail`
True для Gmail-користувачів.

---

## 4. Фільтрація даних

Створено вибірки для окремого аналізу:

- користувачі з Gmail (`gmail_users`);
- компанії, що містять у назві `LLC` або `Ltd`;
- користувачі з міста London;
- компанії з довгими назвами (4+ слів).

---

## 5. Позиційна вибірка (iloc)

Зроблено приклади:
- підвибірки за позиціями таблиці (`iloc[0:11, 2:6]`);
- кожен 10-й рядок датасету;
- випадкова вибірка із 5 рядків.

---

## 6. Групування та статистичний аналіз

Провела дослідження розподілу значень:

### 📌 Частоти міст — `value_counts()`
Дозволяє визначити найпопулярніші міста.

### 📌 Групування за містами:
```python
agg_by_sity = df.groupby('city').agg(
    people_count=('first_name','count'),
    uniq_dom=('email_domain','nunique')
)

У ході групування даних я визначила:

- **кількість людей у кожному місті** — дозволяє зрозуміти географічний розподіл;
- **кількість унікальних email-доменів у кожному місті** — показує різноманітність корпоративної та приватної пошти.

---

### 🌐 Топ доменів

Було проаналізовано найпоширеніші email-домени серед усіх користувачів, що дає уявлення про:

- популярні поштові сервіси,
- корпоративні домени,
- потенційні сегменти аудиторії (наприклад, Gmail-користувачі).

---

## 📤 7. Експорт результатів

Після очищення, аналізу та групування дані було експортовано у зручні формати:

- **`uk500_clean.csv`** — повністю очищений датасет;
- **`gmail_users.csv`** — окрема вибірка користувачів із доменом *gmail.com*;
- **`stats.xlsx`** — Excel-файл із кількома аркушами:
  - **Top Cities** — частотний розподіл міст;
  - **Top Domains** — частотний аналіз доменів.

---

# ✅ Підсумок виконаної роботи

У межах цього проєкту, виконаного мною як аналітиком даних:

### 🔍 Я здійснила:
- повний цикл **очищення даних (Data Cleaning)**;
- розробила власні **функції для стандартизації тексту та телефонних номерів**;
- створила **нові ознаки (feature engineering)**: повне ім’я, домен email, довжина міста тощо;
- виконала **фільтрацію**, **позиційний відбір** та **випадкові вибірки**;
- провела **групування та статистичний аналіз** за доменами та містами;
- експортувала результати в різні формати для подальшого використання.

### 📈 Це дозволило:
- значно підвищити **якість та чистоту даних**;
- забезпечити їх **структурованість і готовність** до подальшого поглибленого аналізу чи побудови моделей машинного навчання;
- сформувати **цінні аналітичні зрізи**, які можуть бути корисними для бізнесу або дослідників.